.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
node_modules/
!ui/dist/
# frontend/dist IS committed: the KiroCrew App Store install path must work
# without node/npm on the target machine (the app serves this bundle).
.env
.hypothesis/
# local-only notes (never published: reference local paths / internal ecosystem)
*.local.md
demo/demo-agents/
