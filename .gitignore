.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
node_modules/
# build output is NEVER tracked on main - the `release` branch carries the
# built bundles for App Store installs (see bin/make-release).
frontend/dist/
ui/dist/
.env
.hypothesis/
# local-only notes (never published: reference local paths / internal ecosystem)
*.local.md
demo/demo-agents/
